import 'package:flutter/material.dart';

class RecipientsNewsletterScreen extends StatelessWidget {
  const RecipientsNewsletterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
