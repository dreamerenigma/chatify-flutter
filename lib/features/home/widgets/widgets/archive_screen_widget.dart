import 'package:flutter/material.dart';

class ArchiveScreenWidget extends StatelessWidget {
  const ArchiveScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
