import 'package:flutter/material.dart';

class RelatedDevicesScreen extends StatelessWidget {
  const RelatedDevicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
