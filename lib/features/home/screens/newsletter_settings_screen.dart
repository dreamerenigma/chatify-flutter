import 'package:flutter/material.dart';

class NewsletterSettingsScreen extends StatelessWidget {
  const NewsletterSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
