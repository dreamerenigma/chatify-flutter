import 'package:flutter/material.dart';

class IncomingVideoCallWidget extends StatelessWidget {
  const IncomingVideoCallWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
