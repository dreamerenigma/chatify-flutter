import 'package:flutter/material.dart';

class LinkGroupScreen extends StatelessWidget {
  const LinkGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
