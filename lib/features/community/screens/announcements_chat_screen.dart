import 'package:flutter/material.dart';

class AnnouncementsChatScreen extends StatelessWidget {
  const AnnouncementsChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
    );
  }
}
