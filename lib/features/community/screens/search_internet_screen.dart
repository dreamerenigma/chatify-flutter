import 'package:flutter/material.dart';

class SearchInternetScreen extends StatelessWidget {
  const SearchInternetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
