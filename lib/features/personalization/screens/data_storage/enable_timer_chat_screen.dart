import 'package:flutter/material.dart';

class EnableTimerChatScreen extends StatelessWidget {
  const EnableTimerChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
